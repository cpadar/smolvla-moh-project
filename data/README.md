# data/

This directory contains data pipeline utilities — NOT raw data.

Raw data should be downloaded separately via:
  python -m mani_skill.utils.download_asset StackPyramid-v1

The data loader in maniskill_dataset.py handles conversion to the
LeRobot/SmolVLA expected format. To be implemented in the data pipeline phase.
